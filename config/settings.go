//all the project const and static info
package config

const ProjectName string = "Web Template"

const SiteRootTemplate = "html/"
const SiteHeader = SiteRootTemplate + "layout/header.html"
const SiteContaint = SiteRootTemplate + "layout/containt.html"
const SiteFooter = SiteRootTemplate + "layout/footer.html"

const PORT = 8081
